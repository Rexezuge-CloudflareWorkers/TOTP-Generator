{
	"name": "totp-generator",
	"version": "1.0.0",
	"private": true,
	"scripts": {
		"cf-typegen": "wrangler types",
		"buildApp": "cd app/ && npm run release",
		"predeploy": "npm install && npm run buildApp",
		"deploy": "wrangler deploy --dry-run",
		"postdeploy": "wrangler deploy",
		"dev": "wrangler dev",
		"start": "wrangler dev"
	},
	"dependencies": {
		"chanfana": "^2.8.3",
		"hono": "^4.10.4",
		"otplib": "^12.0.1",
		"zod": "^4.1.12"
	},
	"devDependencies": {
		"@cloudflare/workers-types": "^4.20251014.0",
		"@types/node": "24.10.0",
		"@types/service-worker-mock": "^2.0.4",
		"wrangler": "^4.45.4"
	}
}