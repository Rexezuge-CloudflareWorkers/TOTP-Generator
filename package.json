{
	"name": "totp-generator",
	"version": "1.0.0",
	"private": true,
	"scripts": {
		"cf-typegen": "wrangler types",
		"buildApp": "cd app/ && npm run release",
		"predeploy": "npm install && npm run buildApp && npm run cf-typegen",
		"deploy": "wrangler deploy --dry-run",
		"postdeploy": "wrangler deploy"
	},
	"dependencies": {
		"chanfana": "^3.3.x",
		"hono": "^4.12.x",
		"otplib": "^13.4.x",
		"zod": "^4.4.x"
	},
	"devDependencies": {
		"@types/node": "25.6.x",
		"wrangler": "^4.87.x"
	}
}